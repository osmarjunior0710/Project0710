import type { Magia } from '../data/rulesets/dnd2024/magias';

/** [PH] [codeimplementation] Adepto de Ritual (Mago, nível 1) — permite
 * conjurar qualquer magia com o marcador Ritual que esteja no Livro de
 * Magias, sem precisar ter preparada e sem gastar Espaço de Magia. Ainda
 * não implementado — ver sdd/sdd-mago-caracteristicas-base.md. */
export function magiasRituaisElegiveis(_livroDeMagias: Magia[], _magiasPreparadas: string[]): Magia[] {
  return [];
}
